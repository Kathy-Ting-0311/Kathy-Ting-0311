# TypeScript Introduction

### What is TypeScript?
- static typing

# How to build the environment for TypeScrit
## Installing the Complier
```
nmp install tyescript --save-dev
```
The compiler is installed in ```node_modules``` directory and can be run with: ```npx tsc ```

```
npx tsc
```
## Configuring the compiler
```
npx tsc --init
```